const saludar = (nombre) =>{ // Función de flecha, recibe un argumento.
    return `Saludos de ${nombre}`;  // Back tic`s temple de string.
}  // Esto es igual a: 'Saludos' + nombre;
console.log(saludar('Ariel'));

